import { PageHeading } from '@/components/shared';

export default function AddPropertyPage() {
  return (
    <div>
      <PageHeading title="Add New Property" subtitle="Create a new listing in a few easy steps." />
    </div>
  );
}
